import React from "react";
import { AuthTemplate, FLoginForm } from "../../components";

const LoginPage: React.FC = () => {
  return (
    <AuthTemplate>
      <FLoginForm />
    </AuthTemplate>
  );
};

export default LoginPage;
